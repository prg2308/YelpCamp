<div class="pagination-div">
    <ul class="pagination justify-content-center">
        <li class="page-item previous">
            <a class="page-link" href="/campgrounds?page=<%= parseInt(page)-1 %>&search=<%= search %>">Previous</a>
        </li>
        <% const pagArrayFirst=[1,2,3] %>
            <!-- <% if(!pagArrayFirst.includes(parseInt(page))){ %>
            <li class="page-item first">
                <a class="page-link"
                    href="/campgrounds?page=1&search=<%= search %>">1</a>
            </li>
            <li class="page-item disabled">
                <span class="page-link">...</span>
            </li>
        <% } %> -->
            <!-- <% if(parseInt(page)!==2){ %>
            <li class="page-item"><a class="page-link"
                href="/campgrounds?page=<%= parseInt(page)-1 %>&search=<%= search %>">
                <%= parseInt(page)-1 %>
            </a>
        </li>
        <% } %> -->
            <li class="page-item"><a class="page-link" href="/campgrounds?page=<%= page %>&search=<%= search %>">
                    <%= page %>
                </a>
            </li>
            <!-- <% if(parseInt(page)!==parseInt(Math.ceil(allCamps.length/6)){ %>
            <li class="page-item"><a class="page-link"
                href="/campgrounds?page=<%= parseInt(page)+1 %>&search=<%= search %>">
                <%= parseInt(page)+1  %>
            </a>
        </li>
        <% } %> -->
            <% const pagArrayLast=[parseInt(Math.ceil(allCamps.length/6)), parseInt(Math.ceil(allCamps.length/6))-1,
                parseInt(Math.ceil(allCamps.length/6))-2 ] %>
                <!-- <% if(!pagArrayLast.includes(parseInt(page))){ %>
            <li class="page-item disabled">
                <span class="page-link">...</span>
            </li>
            <li class="page-item last">
                <a class="page-link"
                    href="/campgrounds?page=<%= Math.ceil(allCamps.length/6) %>&search=<%= search %>"><%= Math.ceil(allCamps.length/6) %></a>
            </li>
        <% } %> -->
                <li class="page-item next">
                    <a class="page-link" href="/campgrounds?page=<%= parseInt(page)+1 %>&search=<%= search %>">Next</a>
                </li>
    </ul>
</div>

<script>
    const campgrounds = { features:<%- JSON.stringify(allCamps) %> }
    const page = <%- JSON.stringify(page) %>
    const previous = document.querySelector('.previous')
    const next = document.querySelector('.next')
    console.log(page)
    if(parseInt(page)===Math.ceil(campgrounds.features.length/6)){
        console.log(page)
        next.classList.add('disabled')
    }
    if(parseInt(page)===1){
        console.log(page)
        previous.classList.add('disabled')
    }
</script>